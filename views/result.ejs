<!DOCTYPE html>
<html lang="en">
<head>
    <title>Results</title>
    <%- include('./_headerImports.ejs') %>
</head>
<body>
    <%- include('./_navbar.ejs') %>
    <div class="container">
        <div class="row">
        
            <div class="col-12 col-md-8 offset-md-2">
          
                <h2>Trajets disponibles le <%=format_date(date)%></h2>
                <ul class="list-group">
                    <% for(var i=0;i<journeys.length;i++){ %>
                    <li class="list-group-item d-flex justify-content-between align-items-center mt-3 p-3 rounded-1">
                        <span><%=journeys[i].departure%> /<%=journeys[i].arrival%> </span>
                        <span>Train departure at: <%=journeys[i].departureTime%></span>
                        <div class="d-flex justify-content-between align-items-center">
                            <strong><%=(journeys[i].price).toFixed(2)%> €</strong>&nbsp;&nbsp;
                            <a href="/addjourney?id=<%=journeys[i].id %>" class="btn btn-danger btn-sm">OK</a>
                        </div>
                    </li>
                    <%}%>
                </ul>
            </div>
        </div>
    </div>
    <%- include('./_jsImports.ejs') %>
    </body>
</html>